[tool.poetry]
name = "oracle"
version = "0.1.0"
description = "Loads Matrix configuration from environment variables and .env"
authors = ["anade-mo <anade-mo@student.42roma.it>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
python-dotenv = "^1.0"
